apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hospath-provisioner
  namespace: argo-cd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: hospath-provisioner
  project: default
  source:
    path: charts/hospath-provisioner
    repoURL: git@github.com:ImDevinC/homelab.git
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: false
