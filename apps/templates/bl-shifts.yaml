apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bl-shifts
  namespace: argo-cd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: bl-shifts
  project: default
  source:
    path: charts/bl-shifts
    repoURL: https://github.com/imdevinc/homelab.git
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      
