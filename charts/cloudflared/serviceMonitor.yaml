apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: cloudflared
  name: cloudflared
spec:
  endpoints:
  - interval: 5m
    path: /metrics
    port: metrics
    scrapeTimeout: 30s
  selector:
    matchLabels:
      app: cloudflared

