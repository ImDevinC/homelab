external-dns:
  domainFilters:
    - "*.int.imdevinc.com"
  policy: upsert-only
  sources: 
  - gateway-httproute
  provider:
    name: pihole
    pihole:
      server: http://192.168.1.249 
      secretName: pihole-password
