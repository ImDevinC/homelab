apiVersion: v1
kind: Service
metadata:
  name: mockery-ai
  labels:
    app: mockery-ai
spec:
  selector:
    app: mockery-ai
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
