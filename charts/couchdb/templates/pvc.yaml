
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: couchdb-hp
  labels:
    app: couchdb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: hostpath-csi
