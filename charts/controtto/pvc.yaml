apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: controtto-claim0
  name: controtto-claim0
  namespace: controtto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
