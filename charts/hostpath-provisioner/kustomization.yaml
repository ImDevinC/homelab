apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hostpath-provisioner

resources:
  - operator.yaml
  - cr.yaml
  - sc.yaml
  - webhook.yaml

images:
  - name: quay.io/kubevirt/hostpath-provisioner-operator
    newTag: v0.25.0
