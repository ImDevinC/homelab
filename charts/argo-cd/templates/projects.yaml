apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: plugins
  namespace: argo-cd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: All non-service plugins that are installed in the cluster
  sourceRepos:
    - '*'
  destinations:
    - server: https://kubernetes.default.svc 
      namespace: '*'