apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: paperless-claim0
  name: paperless-claim0
  namespace: paperless
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
