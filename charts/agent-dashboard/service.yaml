apiVersion: v1
kind: Service
metadata:
  name: agent-dashboard
  namespace: agent-dashboard
  labels:
    app.kubernetes.io/name: agent-dashboard
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-dashboard
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
