apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: recommendarr
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - recommendarr.int.imdevinc.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: recommendarr
          namespace: mediaserver
          port: 3000
