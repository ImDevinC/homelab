---
apiVersion: v1
kind: Service
metadata:
  name: plex-headless
  labels:
    app: plex
spec:
  clusterIP: None
  ports:
    - port: 32400
      targetPort: 32400
      protocol: TCP
      name: plex-port
  selector:
    app: plex
