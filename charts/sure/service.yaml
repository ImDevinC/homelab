apiVersion: v1
kind: Service
metadata:
  labels:
    service: sure
  name: sure
spec:
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    service: sure
