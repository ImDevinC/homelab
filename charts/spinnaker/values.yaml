spinnaker:
  halyard:
    github:
      enabled: true
      accounts:
        - name: imdevinc
          username: ImDevinC
          token: "encrypted:spinnaker-registry-secrets:GHCR"
  dockerRegistryAccountSecret: spinnaker-registry-secrets
  dockerRegistries:
    - name: GHCR
      address: https://ghcr.oi
      username: ImDevinC
  ingress:
    enabled: true
    className: nginx
    host: spinnaker.collins.home
  openldap:
    image:
      tag: 1.5.0
    persistence:
      storageClass: local-storage
    env:
      DISABLE_CHOWN: "true"
      KEEP_EXISTING_CONFIG: "true"